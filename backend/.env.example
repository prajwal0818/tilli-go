DATABASE_URL=postgresql://tilligo:tilligo@localhost:5436/tilligo
REDIS_HOST=localhost
REDIS_PORT=6381
JWT_SECRET=change-me-in-production
API_PORT=3001
FRONTEND_URL=http://localhost:3000
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ACK_TOKEN_SECRET=change-me-in-production

# ── Microsoft OAuth (Azure AD / Entra ID) ────────────────
# Leave blank to disable Microsoft sign-in
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=http://localhost:3001/api/auth/microsoft/callback

# Token encryption key for storing OAuth tokens at rest (min 32 chars)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
TOKEN_ENCRYPTION_KEY=
